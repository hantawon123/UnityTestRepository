-- 신고를 누가 검토했는지 남깁니다.
--
-- V9 를 만들 때는 인증이 없어서 넣지 않았습니다. 어떤 값을 넣어도 스스로 신고한
-- 이름일 뿐이라 확인할 수 없는 "누가 결정했다"는 없느니만 못했습니다.
-- S15P21D205-542 로 관리자 인증이 생겨 이제 검증할 수 있습니다.
--
-- 지금은 계정이 하나라 값이 늘 같습니다. 그래도 넣어두는 이유는 사람마다 계정을
-- 나눌 일이 생겼을 때 스키마를 다시 고치지 않아도 되기 때문입니다.
--
-- 아직 검토하지 않은 행은 NULL 입니다. reviewed_at 과 같은 자리이고, 둘은 항상
-- 함께 채워집니다 - 따로 두면 "처리했다는데 누가 언제인지 모르는" 행이 생깁니다.
ALTER TABLE user_reports
    ADD COLUMN reviewed_by VARCHAR(32) NULL;
