# Jenkins systemd 드롭인.
# 배치 위치: /etc/systemd/system/jenkins.service.d/override.conf
#
# 패키지 기본값은 0.0.0.0:8080 입니다. 8080은 앱 컨테이너가 이미 쓰고 있고,
# Jenkins를 인터넷에 직접 노출할 이유도 없습니다. nginx가 443에서 받아
# /jenkins/ 를 이쪽으로 프록시합니다.
#
# 유닛의 ExecStart는 /usr/bin/jenkins 래퍼이고, 아래 변수들을 읽습니다.
# 패키지 유닛에 주석으로 나열돼 있는 지원 목록에서 가져온 이름입니다.

[Service]
# 앱과 충돌하지 않는 포트.
Environment="JENKINS_PORT=9090"

# 루프백에만 바인딩합니다. ufw가 9090을 막고 있지만 그것에만 의존하지
# 않습니다. Docker가 ufw를 우회하는 것에서 봤듯 방화벽은 절대적이지 않고,
# Jenkins는 docker 소켓 권한을 가지므로 뚫리면 호스트 전체가 넘어갑니다.
Environment="JENKINS_LISTEN_ADDRESS=127.0.0.1"

# nginx가 /jenkins/ 로 프록시하므로 Jenkins도 같은 프리픽스를 알아야 합니다.
# 없으면 Jenkins가 링크를 / 부터 생성해 로그인 이후 UI가 전부 깨집니다.
Environment="JENKINS_PREFIX=/jenkins"

# 체크아웃할 때 Git LFS 실제 파일을 내려받지 않습니다. 포인터 파일만 둡니다.
#
# 이 파이프라인은 backend/ 만 빌드하고 Unity 에셋(LFS 3천 개)은 한 바이트도 읽지 않습니다.
# 그런데 git checkout 은 기본으로 LFS 스머지 필터를 돌려 그 전부를 GitLab 에서 받아 옵니다.
# 2026-09-07 저녁, 클라이언트 쪽이 애니메이션 클립을 한꺼번에 올린 뒤 그 내려받기가 10분을
# 넘겨 모든 MR 빌드가 "Checkout SCM" 에서 타임아웃으로 죽었습니다. 코드는 아무 상관이 없었습니다.
#
# Jenkins 가 실행하는 git 은 이 환경변수를 상속받아 스머지를 건너뜁니다. 재시작이 필요합니다.
# 재시작 없이 바로 적용하려면 jenkins 사용자의 gitconfig 로 같은 효과를 냅니다.
# deploy/README.md 의 "Jenkins 체크아웃이 10분 타임아웃으로 죽으면" 절을 보세요.
Environment="GIT_LFS_SKIP_SMUDGE=1"
